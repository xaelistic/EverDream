/** NFT minting UI is disabled for launch; backend edge function remains deployed. */
export const FEATURE_NFT_UI_ENABLED = false;

/** 3D simulacrum, mesh pipeline, and related immersive UI. */
export const FEATURE_MESH_UI_ENABLED = false;

/** WebXR / VR viewer entry points. */
export const FEATURE_VR_UI_ENABLED = false;

/** App skin picker and theme-variant A/B UI. */
export const FEATURE_SKINS_UI_ENABLED = false;

/** Require email/password or OAuth sign-in instead of anonymous auth. */
export const FEATURE_REQUIRE_AUTH = true;